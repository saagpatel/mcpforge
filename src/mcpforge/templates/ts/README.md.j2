# {{ plan.name }}

{{ plan.description }}

Generated by mcpforge.

## Tools

{% for tool in plan.tools %}
### `{{ tool.name }}`

{{ tool.description }}

{% endfor %}
## Setup

```bash
npm install
npm test
npm run start
```

{% if plan.env_vars %}
## Environment

Copy `.env.example` to `.env` and fill in:

{% for var in plan.env_vars %}
- `{{ var }}`
{% endfor %}
{% endif %}

## MCP Client Configuration

```json
{
  "mcpServers": {
    "{{ plan.slug }}": {
      "command": "npm",
      "args": ["--prefix", ".", "run", "start"]
    }
  }
}
```
